export enum TripRole {
  OWNER = 'OWNER',
  EDITOR = 'EDITOR',
  VIEWER = 'VIEWER',
}

export enum ActivityType {
  TRAVEL = 'TRAVEL',
  VISIT = 'VISIT',
  STAY = 'STAY',
  FOOD = 'FOOD',
  EVENT = 'EVENT',
}

export enum CommentContext {
  ACTIVITY = 'ACTIVITY',
  DAY = 'DAY',
}

export enum InviteStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  DISABLED = 'DISABLED',
}
